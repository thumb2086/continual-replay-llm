/**
 * zllm — programmatic API (future: direct JS calls to compression engine)
 * For now, all functionality goes through the CLI.
 */
module.exports = {
  version: "0.1.0",
  // Future: encode(), decode(), bench() as JS functions
  // communicating with Python backend via IPC or child_process
};
